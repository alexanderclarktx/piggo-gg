{
  "name": "piggo-gg",
  "private": true,
  "workspaces": [
    "core",
    "web",
    "server"
  ],
  "scripts": {
    "prod": "bun gen && bun --cwd server start",
    "compile": "rm -rf ./dist && bun build web/src/index.ts --outdir docs --minify && cp -r web/res/* docs/",
    "dev": "bun --cwd web dev & bun --cwd server dev && fg",
    "gen": "bun --cwd server prisma:generate",
    "db": "bun --cwd server prisma:local",
    "branch": "git checkout main && git pull && git branch -D $(whoami) ; git checkout -b $(whoami)",
    "pages": "rm -r ./docs && mkdir docs && git restore docs/CNAME && ENV=production bun compile",
    "ship": "bun pages && git add . && git commit -m 'ship' && git push",
    "b": "bun branch",
    "s": "bun ship 2>/dev/null"
  },
  "license": "MIT+CC",
  "dependencies": {
    "typescript": "5.8.3"
  },
  "devDependencies": {
    "ts-loader": "9.4.2"
  }
}
