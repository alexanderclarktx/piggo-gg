{
  "name": "piggo-gg",
  "private": true,
  "workspaces": ["core", "games", "web", "server", "electron"],
  "scripts": {
    "dev": "bun --cwd web dev & bun --cwd server dev && fg",
    "branch": "git checkout main && git pull && git branch -D dev && git checkout -b dev && git push -u origin dev",
    "server": "bun --cwd server start",
    "docs": "bun pages",
    "pages": "ENV=production bun --cwd web build && rm -r ./docs && mkdir docs && cp -r ./web/dist/* ./docs/ && git restore docs/CNAME"
  },
  "license": "MIT+CC",
  "dependencies": {
    "typescript": "5.5.4"
  },
  "devDependencies": {
    "@types/bun": "^1.1.6",
    "copy-webpack-plugin": "11.0.0",
    "electron": "^31.3.1",
    "ts-loader": "9.4.2",
    "tsc-alias": "^1.8.10",
    "webpack": "5.75.0",
    "webpack-cli": "5.0.1",
    "webpack-dev-server": "4.11.1"
  }
}
