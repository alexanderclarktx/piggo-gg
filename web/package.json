{
  "name": "@piggo-gg/web",
  "main": "./main.ts",
  "scripts": {
    "build": "rm -rf ./dist && NODE_ENV=production bun x webpack",
    "bbuild": "rm -rf ./dist && NODE_ENV=production bun build src/index.tsx --outdir dist --production && cp -r res/* dist/",
    "dev": "bun x webpack serve --port 8000"
  },
  "dependencies": {
    "@sethwebster/react-fps-counter": "^0.0.19",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.4.0",
    "react-modal": "^3.16.1"
  },
  "devDependencies": {
    "@types/google.accounts": "^0.0.15",
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "@types/react-modal": "^3.13.1",
    "webpack": "^5.75.0"
  }
}
