{
  "name": "@piggo-gg/web",
  "main": "./main.ts",
  "scripts": {
    "bbuild": "rm -rf ./dist && bun build src/index.ts --outdir dist --minify && cp -r res/* dist/ && cp src/index.html dist/ && cp src/index.css dist/"
  },
  "dependencies": {
    "@piggo-gg/lex": "1.0.4",
    "@sethwebster/react-fps-counter": "^0.0.19"
  },
  "devDependencies": {
    "@types/google.accounts": "^0.0.15",
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "webpack": "^5.75.0"
  }
}
